# MiMiTrends documentation assets

- `AppIcon-1024.png` — application icon source used by the README and packaging tasks.
- `MainWindow.png` — current main-window screenshot used by the project README.

The main project README also documents the hybrid market-data pipeline: Yahoo Finance supplies cached history and fallback data, while an optional user-owned Finnhub key enables live WebSocket trade aggregation. European Yahoo delay labels are intentionally visible in the scanner.
