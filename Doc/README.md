# MiMiTrends documentation assets

- `AppIcon-1024.png` — application icon source used by the README and packaging tasks.
- `LiveRadar.png` — current compact Live radar with full-row rapid-crash emphasis and active scan context.

The retired multi-panel workspace, Positive watch, old settings dialog, broker chooser, and historical
packaging-output screenshots were removed because they no longer represent the current product UI.

The main project README also documents the hybrid market-data pipeline, focused five-second hover
refresh, external performance discovery, repeating-cycle markers, adaptive long-candidate fallback,
broker-trade overlays, and three-digit native package versioning.
